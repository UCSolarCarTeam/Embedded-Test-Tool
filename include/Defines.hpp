#ifndef DEFINES_HPP
#define DEFINES_HPP

#define DELTA
// #define NOT_DELTA // Whatever the next car is
static const int NUMBER_OF_PINS = 27;

extern const PinName pinNameArray[NUMBER_OF_PINS];

// Used for pin-number to array-index conversion
extern const int PIN_OFFSET;

#endif // DEFINES_HPP
